<%- include("partials/header", { bot, user, path, title: "Select server" }) %>

<br></br>
<h1 style="text-align: center;">Select a Server</h1>
<br></br>
<div style="display: flex; flex-wrap: wrap; justify-content: center;">
  <% user.guilds.forEach(guild => {
      const permsOnGuild = new perms(guild.permissions_new);
      if (!permsOnGuild.has(perms.FLAGS.MANAGE_GUILD) || !bot.guilds.cache.get(guild.id)) return;
  %>
  <a href="/dashboard/<%- guild.id %>" style="text-decoration: none;">
    <div class="card" style="max-width: 18rem; margin: 2rem; min-width: 10em;">
    <%- guild.icon ? `<img src="https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}" class="card-img-top" style="width: 18rem; border-radius: 50%;">` : `<img src="/assets/images/placeholder.png" class="card-img-top" style="max-width: 18rem; border-radius: 50%;">` %>
      <div class="card-body">
        <br></br>
        <h2 class="card-title"><%= guild.name %></h2>
      </div>
    </div>
  </a>
  <%
      });
  %>
</div>

<%- include("partials/footer") %>